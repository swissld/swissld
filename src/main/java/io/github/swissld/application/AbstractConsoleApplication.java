package io.github.swissld.application;

/**
 * <h3>AbstractConsoleApplication</h3>
 * 
 * @version 0.1.0
 * @since 0.1.0
 */
public abstract class AbstractConsoleApplication<T extends AbstractConsoleApplication<T, E>, E extends Exception> extends AbstractApplication<T, E> implements ConsoleApplicationInterface<T, E>
	{
	/*
	public enum Color
		{
		RED
		}
	*/

	/*
	//protected final Scanner scanner = new Scanner(System.in);

	

	// System.out.println("\u001B[31m" + "and now the text is red" + "\u001B[0m");

	

	@Override
	public final void red()
		{
		command(RED);
		}

	//@Override
	public final void red(final String message)
		{
		command(RED);
		System.out.print(message);
		reset();
		}

	
	*/
	}
